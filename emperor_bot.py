import telebot
import time
import random
from datetime import datetime, timedelta, timezone
from flask import Flask
import threading

# Создаем Flask приложение для здоровья
app = Flask(__name__)

@app.route('/')
def health_check():
    return "🔄 Хорарный Император работает!", 200

def run_flask():
    app.run(host='0.0.0.0', port=5000, debug=False)

flask_thread = threading.Thread(target=run_flask, daemon=True)
flask_thread.start()

BOT_TOKEN = "7166686748:AAFnyfjq5UsunijP_p8HQiYeKHh3qoAM5RA"
bot = telebot.TeleBot(BOT_TOKEN)

class ProfessionalAstrologer:
    def __init__(self):
        self.experience = 0
        self.waiting_for_clarification = {}
    
    def analyze_user_intent(self, question):
        """УМНОЕ распознавание намерений пользователя"""
        question_lower = question.lower()
        
        # Распознаем вопросы про ЛС и конфиденциальность
        if any(word in question_lower for word in ['личн', 'приватн', 'конфиденц', 'секретн', 'тайна', 'скрытн']):
            return "PRIVACY_REQUEST"
        elif any(word in question_lower for word in ['личное', 'в лс', 'в личку', 'в личные', 'приватно']):
            return "PRIVATE_QUESTION"
        elif any(word in question_lower for word in ['как задать', 'как спросить', 'как работать', 'инструкц', 'помощь']):
            return "HELP_REQUEST"
        elif any(word in question_lower for word in ['что ты умеешь', 'возможност', 'функцион']):
            return "CAPABILITIES"
        
        # Обычная классификация вопросов
        if any(word in question_lower for word in ['деньг', 'финанс', 'придут', 'зарплат', 'преми', 'долг', 'вернут', 'кредит']):
            return "ФИНАНСЫ", "💰"
        elif any(word in question_lower for word in ['любит', 'отношен', 'брак', 'замуж', 'встреч', 'парень', 'мужчин', 'девушк', 'чувств', 'влюблен']):
            return "ОТНОШЕНИЯ", "💖" 
        elif any(word in question_lower for word in ['здоров', 'болез', 'лечен', 'врач', 'больниц', 'самочувств', 'анализ', 'диагноз']):
            return "ЗДОРОВЬЕ", "🏥"
        elif any(word in question_lower for word in ['работ', 'карьер', 'должност', 'бизнес', 'проект', 'начальник', 'устроюсь']):
            return "КАРЬЕРА", "🚀"
        elif any(word in question_lower for word in ['поезд', 'путешеств', 'переезд', 'отпуск']):
            return "ПУТЕШЕСТВИЯ", "✈️"
        else:
            return "ОБЩИЙ", "🔮"
    
    def generate_smart_response(self, question, user_id=None, chat_type="private"):
        """УМНЫЙ ответ с распознаванием намерений"""
        
        # Проверяем специальные намерения
        intent = self.analyze_user_intent(question)
        
        if intent == "PRIVACY_REQUEST":
            return self._get_privacy_response()
        elif intent == "HELP_REQUEST":
            return self._get_help_response(chat_type)
        elif intent == "CAPABILITIES":
            return self._get_capabilities_response()
        elif intent == "PRIVATE_QUESTION":
            return self._get_private_question_response()
        
        # Если это обычный вопрос - анализируем астрологически
        moon_sign = get_random_zodiac()
        sun_sign = get_random_zodiac()
        
        if len(question.strip()) < 5 or question.lower() in ['император', 'бот', 'привет']:
            return self._get_greeting_response(moon_sign, sun_sign, chat_type)
        
        if isinstance(intent, tuple):
            question_type, emoji = intent
        else:
            question_type, emoji = "ОБЩИЙ", "🔮"
        
        # Если вопрос слишком общий
        if self._is_too_general(question):
            if user_id:
                self.waiting_for_clarification[user_id] = question_type
            return self._ask_for_clarification(question_type, moon_sign, sun_sign)
        
        # ПРОФЕССИОНАЛЬНЫЙ АНАЛИЗ
        return self._get_professional_reading(question, moon_sign, sun_sign, question_type)
    
    def _get_privacy_response(self):
        """Ответ на вопросы о конфиденциальности"""
        responses = [
            "🔒 *КОНФИДЕНЦИАЛЬНОСТЬ ГАРАНТИРОВАНА!*\n\n"
            "💫 *Ваши варианты:*\n"
            "• 💬 Задать вопрос здесь\n"  
            "• 🔒 Написать 'Личное [вопрос]' для ответа в ЛС\n"
            "• 💌 Написать мне в личные сообщения\n\n"
            "*Ваша тайна в полной безопасности!* ✨",
            
            "🤫 *ПОЛНАЯ КОНФИДЕНЦИАЛЬНОСТЬ!*\n\n"
            "*Как задать вопрос:*\n"
            "💬 Публично - здесь и сейчас\n"
            "🔒 Приватно - через 'Личное [вопрос]'\n"  
            "💌 Анонимно - в моих личных сообщениях\n\n"
            "*Выбирайте удобный способ!* 🌟"
        ]
        verdict = "🔒"
        analysis = random.choice(responses)
        strategy = "Выберите удобный способ общения"
        return verdict, analysis, strategy
    
    def _get_help_response(self, chat_type):
        """Ответ на запросы помощи"""
        if chat_type == "private":
            help_text = """
💫 *КАК РАБОТАЕТ ХОРАРНЫЙ ИМПЕРАТОР*

*Просто задайте вопрос о:*
💰 *Финансах* - придут ли деньги, вернут ли долг
💖 *Отношениях* - любит ли человек, будет ли будущее  
🏥 *Здоровье* - поправлюсь ли, поможет ли лечение
🚀 *Карьере* - устроюсь ли на работу, будет ли повышение

*Примеры вопросов:*
• «Придут ли мне 5000 руб до пятницы?»
• «Любит ли меня Александр?»
• «Получу ли я эту работу?»

*Задавайте конкретные вопросы - получу точные ответы!* 🔮
"""
        else:
            help_text = """
👑 *КАК ЗАДАТЬ ВОПРОС В ГРУППЕ*

💬 *Публичный вопрос:* просто напишите вопрос
🔒 *Приватный ответ:* напишите «Личное [ваш вопрос]»
💌 *Полная конфиденциальность:* напишите мне в ЛС

*Примеры:*
• «Когда придут мне деньги?»
• «Личное Любит ли меня муж?»
• *Написать в ЛС:* @HoraryEmperorBot

*Ваша тайна в безопасности!* 🔒
"""
        verdict = "💫"
        analysis = help_text
        strategy = "Задайте конкретный вопрос для анализа"
        return verdict, analysis, strategy
    
    def _get_capabilities_response(self):
        """Ответ на вопрос о возможностях"""
        capabilities = """
🔮 *МОИ ВОЗМОЖНОСТИ ХОРАРНОЙ АСТРОЛОГИИ*

*Я анализирую по хорарным картам:*

💰 *ФИНАНСЫ*
• Придут ли деньги, когда и откуда
• Вернут ли долг, окупятся ли инвестиции
• Успешен ли бизнес/проект

💖 *ОТНОШЕНИЯ*  
• Любит ли человек, какие чувства испытывает
• Вернется ли партнер, будет ли примирение
• Будет ли брак/будущее вместе

🏥 *ЗДОРОВЬЕ*
• Поправлюсь ли, как скоро выздоровею
• Поможет ли лечение/врач
• Что укрепит здоровье

🚀 *КАРЬЕРА*
• Устроюсь ли на работу, будет ли повышение
• Успешен ли проект, стоит ли менять работу
• Отношения с начальством/коллегами

✨ *Задавайте конкретные вопросы - получу точные ответы!*
"""
        verdict = "🔮"
        analysis = capabilities
        strategy = "Выберите тему и задайте вопрос"
        return verdict, analysis, strategy
    
    def _get_private_question_response(self):
        """Ответ на запрос приватного вопроса"""
        response = """
💌 *ПРИВАТНЫЙ ВОПРОС - ОТВЕТ В ЛС!*

🔒 *Для конфиденциального анализа:*
1. Напишите мне в личные сообщения
2. Или используйте «Личное [ваш вопрос]» здесь

*Пример приватного вопроса:*
«Личное Вернется ли ко мне бывший муж?»

*Ваша тайна будет в полной безопасности!* 🤫

✨ *Или задайте вопрос прямо здесь для публичного ответа*
"""
        verdict = "💌"
        analysis = response
        strategy = "Выберите удобный способ"
        return verdict, analysis, strategy
    
    def _get_greeting_response(self, moon_sign, sun_sign, chat_type):
        """Умное приветствие в зависимости от типа чата"""
        if chat_type == "private":
            responses = [
                f"🔮 *Привет! Я Хорарный Император!*\n\n"
                f"💫 *Луна в {moon_sign} готова к анализу!*\n\n"
                f"*Задайте вопрос о:*\n"
                f"• 💰 Финансах и деньгах\n"
                f"• 💖 Отношениях и чувствах\n"  
                f"• 🏥 Здоровье и самочувствии\n"
                f"• 🚀 Карьере и работе\n\n"
                f"*Пример:* «Придут ли мне деньги до пятницы?»\n\n"
                f"🔒 *Ваши вопросы конфиденциальны!*",
                
                f"👑 *Профессиональный Хорарный Император к вашим услугам!*\n\n"
                f"*Солнце в {sun_sign} готово к анализу вашей ситуации!*\n\n"
                f"*Что вас волнует?*\n"
                f"💬 Задайте вопрос прямо здесь\n"
                f"🔒 Конфиденциальность гарантирована\n\n"
                f"*Пример вопроса:* «Любит ли меня этот человек?»"
            ]
        else:
            responses = [
                f"🔮 *Привет! Я Хорарный Император!*\n\n"
                f"💬 *Можешь задать вопрос прямо здесь*\n"
                f"🔒 *Или написать 'Личное [вопрос]' для ответа в ЛС*\n"  
                f"💌 *Или написать мне в личные сообщения*\n\n"
                f"*Что выберешь?* ✨",
                
                f"👑 *Хорарный Император в вашем распоряжении!*\n\n"
                f"*Варианты общения:*\n"
                f"💬 Публичный вопрос - здесь\n"
                f"🔒 Приватный ответ - через 'Личное [вопрос]'\n"
                f"💌 Полная конфиденциальность - в моих ЛС\n\n"
                f"*Выбирайте!* 🌟"
            ]
        
        verdict = "👑"
        analysis = random.choice(responses)
        strategy = "Задайте вопрос для астрологического анализа"
        return verdict, analysis, strategy

    # Остальные методы класса остаются без изменений
    def _is_too_general(self, question):
        question_lower = question.lower()
        too_general = ['вопрос про деньги', 'про финансы', 'про отношения', 'про работу', 'про здоровье', 'про карьеру', 'про это']
        return any(pattern in question_lower for pattern in too_general)
    
    def _ask_for_clarification(self, question_type, moon_sign, sun_sign):
        clarifications = {
            "ФИНАНСЫ": [f"💎 *Уточните финансовый вопрос*\n\nЛуна в {moon_sign} готова к анализу:\n\n• Конкретные суммы и сроки\n• Источник денег\n• Финансовые решения",],
            "ОТНОШЕНИЯ": [f"💖 *Уточните вопрос отношений*\n\nСолнце в {sun_sign} готово к анализу:\n\n• Конкретный человек\n• Ситуация\n• Будущее отношений",],
            "ЗДОРОВЬЕ": [f"🏥 *Уточните вопрос здоровья*\n\nЛуна в {moon_sign} готова к диагностике:\n\n• Симптомы/состояние\n• Лечение\n• Профилактика",]
        }
        default_msg = f"🔮 Задай конкретный вопрос для профессионального ответа!"
        options = clarifications.get(question_type, [default_msg])
        verdict = "🎯"
        analysis = random.choice(options)
        strategy = "Уточни вопрос для точного анализа"
        return verdict, analysis, strategy
    
    def _get_professional_reading(self, question, moon_sign, sun_sign, question_type):
        if question_type == "ФИНАНСЫ":
            return self._financial_horary_analysis(question, moon_sign, sun_sign)
        elif question_type == "ОТНОШЕНИЯ":
            return self._relationship_horary_analysis(question, moon_sign, sun_sign)
        elif question_type == "ЗДОРОВЬЕ":
            return self._health_horary_analysis(question, moon_sign, sun_sign)
        else:
            return self._general_horary_analysis(question, moon_sign, sun_sign)
    
    def _financial_horary_analysis(self, question, moon_sign, sun_sign):
        current_time = datetime.now(timezone.utc) + timedelta(hours=3)
        time_str = current_time.strftime('%H:%M, %d %B %Y')
        
        analysis_text = f"""
*Астрологический анализ*

*Время:* {time_str}
*Вопрос:* «{question}»

---

*Карта вопроса:*

• *Ты (1-й дом):* в {self._get_rising_sign()} 
• *Управитель — {self._get_ruler()}* в {random.randint(1, 30)}° {self._get_sign()}

• *Твои деньги (2-й дом):* в {self._get_second_house()} 
• *Управитель — {self._get_money_ruler()}* в {random.randint(1, 30)}° {self._get_sign()}

• *Общий сигнификатор денег:* 
  · Луна в {moon_sign}
  · Солнце в {sun_sign}

---

*Ключевые аспекты:*
"""
        
        question_lower = question.lower()
        if any(word in question_lower for word in ['придут', 'получу', 'когда']):
            analysis_text += self._analyze_money_timing(moon_sign, sun_sign)
        elif any(word in question_lower for word in ['долг', 'вернут']):
            analysis_text += self._analyze_debt_return(moon_sign, sun_sign)
        else:
            analysis_text += self._analyze_general_finance(moon_sign, sun_sign)
        
        analysis_text += f"\n---\n*Вердикт:*\n\n{self._get_finance_verdict(moon_sign, sun_sign, question_lower)}"
        
        verdict = self._get_finance_verdict_symbol(moon_sign, sun_sign)
        strategy = self._get_finance_strategy(moon_sign)
        
        return verdict, analysis_text, strategy

    # Остальные методы анализа остаются без изменений
    def _relationship_horary_analysis(self, question, moon_sign, sun_sign):
        return self._general_horary_analysis(question, moon_sign, sun_sign)
    
    def _health_horary_analysis(self, question, moon_sign, sun_sign):
        return self._general_horary_analysis(question, moon_sign, sun_sign)
    
    def _general_horary_analysis(self, question, moon_sign, sun_sign):
        current_time = datetime.now(timezone.utc) + timedelta(hours=3)
        time_str = current_time.strftime('%H:%M, %d %B %Y')
        
        analysis_text = f"""
*Астрологический анализ*

*Время:* {time_str}
*Вопрос:* «{question}»

---

*Карта вопроса:*

• *1-й дом (Ты):* в {self._get_rising_sign()}
• *Управитель — {self._get_ruler()}* в {random.randint(1, 30)}° {self._get_sign()}

• *Соответствующий дом вопроса:* в благоприятном положении
• *Общие сигнификаторы:* 
  · Луна в {moon_sign}
  · Солнце в {sun_sign}

---

*Ключевые аспекты:*

· Луна в {moon_sign} — эмоциональный фон стабильный
· Солнце в {sun_sign} — волевая энергия на хорошем уровне  
· Управитель вопроса в сильном положении — ситуация развивается благоприятно

---

*Вердикт:*

На основании карты, ситуация имеет ПОЗИТИВНУЮ динамику. 
Текущие энергетические потоки поддерживают ваши намерения.
"""
        
        verdict = "🌟"
        strategy = "Продолжайте действовать в выбранном направлении"
        
        return verdict, analysis_text, strategy

    # Вспомогательные методы
    def _get_rising_sign(self): return random.choice(['Овне', 'Тельце', 'Близнецах', 'Раке', 'Льве', 'Деве', 'Весах', 'Скорпионе', 'Стрельце', 'Козероге', 'Водолее', 'Рыбах'])
    def _get_ruler(self): return random.choice(['Марс', 'Венера', 'Меркурий', 'Луна', 'Солнце', 'Меркурий', 'Венера', 'Марс', 'Юпитер', 'Сатурн', 'Уран', 'Нептун'])
    def _get_sign(self): return random.choice(['Овна', 'Тельца', 'Близнецов', 'Рака', 'Льва', 'Девы', 'Весов', 'Скорпиона', 'Стрельца', 'Козерога', 'Водолея', 'Рыб'])
    def _get_second_house(self): return self._get_rising_sign()
    def _get_money_ruler(self): return self._get_ruler()
    
    def _analyze_money_timing(self, moon_sign, sun_sign):
        aspects = []
        if moon_sign in ['Телец', 'Рак', 'Козерог']:
            aspects.append(f"· Луна в {moon_sign} — благоприятный знак для финансов. Деньги могут прийти в течение 3-7 дней.")
        else:
            aspects.append(f"· Луна в {moon_sign} — возможны небольшие задержки. Ожидайте поступления в течение 1-2 недель.")
        aspects.append("· Управитель 2-го дома ретроградный — возможны задержки из-за документов.")
        return "\n".join(aspects)
    
    def _analyze_debt_return(self, moon_sign, sun_sign):
        return f"· Луна в {moon_sign} — возврат долга вероятен, но требует времени.\n· Управитель 2-го дома делает аспект к управителю 8-го — долг будет возвращен."
    
    def _analyze_general_finance(self, moon_sign, sun_sign):
        return f"· Луна в {moon_sign} — эмоциональное отношение к деньгам стабильное.\n· Солнце в {sun_sign} — финансовая энергия на хорошем уровне."
    
    def _get_finance_verdict(self, moon_sign, sun_sign, question_lower):
        if 'сегодня' in question_lower:
            return "На основании карты, получение денег сегодня МАЛОВЕРОЯТНО. Ретроградный управитель 2-го дома указывает на задержки."
        else:
            return "Финансовые потоки требуют ТЕРПЕНИЯ. Возможны небольшие задержки из-за внешних обстоятельств."
    
    def _get_finance_verdict_symbol(self, moon_sign, sun_sign):
        return "💰" if moon_sign in ['Телец', 'Рак', 'Козерог'] else "⏳"
    
    def _get_finance_strategy(self, moon_sign):
        return random.choice(["Проверьте финансовые каналы", "Составьте план распределения средств", "Проявите терпение - деньги в пути"])

# Создаем экземпляр астролога
astrologer = ProfessionalAstrologer()

def get_moscow_time():
    utc_time = datetime.now(timezone.utc)
    moscow_time = utc_time + timedelta(hours=3)
    return moscow_time.strftime('%H:%M, %d.%m.%Y')

def get_random_zodiac():
    signs = ['Овен', 'Телец', 'Близнецы', 'Рак', 'Лев', 'Дева', 'Весы', 'Скорпион', 'Стрелец', 'Козерог', 'Водолей', 'Рыбы']
    return random.choice(signs)

# УМНАЯ ОБРАБОТКА ГРУПП
@bot.message_handler(chat_types=['supergroup', 'group'])
def handle_group_message(message):
    try:
        if message.text:
            question = None
            
            # Автоматическое приглашение в ЛС при упоминании
            if '@HoraryEmperorBot' in message.text:
                bot_text = message.text.replace('@HoraryEmperorBot', '').strip()
                
                if not bot_text or len(bot_text) < 3:
                    try:
                        welcome_msg = "🔮 *Привет! Я Хорарный Император!*\n\n💬 *Можешь задать вопрос прямо здесь*\n🔒 *Или написать 'Личное [вопрос]' для ответа в ЛС*\n💌 *Или написать мне в личные сообщения*\n\n*Что выберешь?* ✨"
                        bot.send_message(message.from_user.id, welcome_msg)
                        bot.reply_to(message, "📨 *Приглашение в личные сообщения отправлено!*")
                    except:
                        group_msg = "🔮 *Привет! Я Хорарный Император!*\n\n💬 *Задай вопрос прямо здесь*\n🔒 *Или напиши 'Личное [вопрос]' для ответа в ЛС*\n💌 *Или напиши мне в личные сообщения*\n\n*Что выберешь?* ✨"
                        bot.reply_to(message, group_msg)
                    return
                
                if bot_text.lower().startswith('личное'):
                    question = bot_text.replace('личное', '').strip()
                    if question:
                        try:
                            verdict, analysis, strategy = astrologer.generate_smart_response(question, message.from_user.id, "group")
                            private_msg = f"🔒 *ЛИЧНЫЙ АНАЛИЗ*\n\n{analysis}\n\n🎯 *Рекомендация:* {strategy}"
                            bot.send_message(message.from_user.id, private_msg)
                            bot.reply_to(message, "📨 *Конфиденциальный анализ отправлен в личные сообщения!*")
                        except:
                            bot.reply_to(message, "💌 *Для конфиденциального анализа напишите мне в ЛС:* @HoraryEmperorBot")
                    return
                else:
                    question = bot_text
            
            # Обработка специальных команд
            elif message.text.lower() in ['император', 'бот', 'император?', 'бот?', 'астролог']:
                verdict, analysis, strategy = astrologer.generate_smart_response("помощь", chat_type="group")
                bot.reply_to(message, f"{verdict} {analysis}")
                return
            
            elif message.text.lower().startswith('личное'):
                question = message.text.replace('личное', '').strip()
                if question:
                    try:
                        verdict, analysis, strategy = astrologer.generate_smart_response(question, message.from_user.id, "group")
                        private_msg = f"🔒 *ЛИЧНЫЙ АНАЛИЗ*\n\n{analysis}\n\n🎯 *Рекомендация:* {strategy}"
                        bot.send_message(message.from_user.id, private_msg)
                        bot.reply_to(message, "📨 *Конфиденциальный анализ отправлен в личные сообщения!*")
                    except:
                        bot.reply_to(message, "💌 *Для конфиденциального анализа напишите мне в ЛС:* @HoraryEmperorBot")
                return
            
            # Публичный анализ
            elif '?' in message.text and len(message.text) > 10:
                question = message.text.strip()
            
            if question and len(question) > 5:
                verdict, analysis, strategy = astrologer.generate_smart_response(question, message.from_user.id, "group")
                response = f"{verdict} *ПРОФЕССИОНАЛЬНЫЙ АНАЛИЗ*\n⏰ {get_moscow_time()}\n\n{analysis}\n\n🎯 *Рекомендация:* {strategy}\n\n💌 *Для конфиденциальных вопросов - пишите в ЛС*"
                bot.reply_to(message, response)
                
    except Exception as e:
        print(f"Ошибка в группе: {e}")

# ЛИЧНЫЕ СООБЩЕНИЯ
@bot.message_handler(func=lambda message: True, chat_types=['private'])
def handle_private_message(message):
    if message.text.startswith('/'):
        if message.text == '/start':
            start_text = "🔮 *Привет! Я Хорарный Император!*\n\n💫 *Задайте вопрос о финансах, отношениях, здоровье или карьере для глубинного астрологического анализа!*\n\n🔒 *Ваши вопросы конфиденциальны!*"
            bot.reply_to(message, start_text)
        return
    
    try:
        verdict, analysis, strategy = astrologer.generate_smart_response(message.text, message.from_user.id, "private")
        
        response = f"""
{verdict} *ПРОФЕССИОНАЛЬНЫЙ АНАЛИЗ*
⏰ {get_moscow_time()}
🔒 *Конфиденциально*

{analysis}

🎯 *Рекомендация:* {strategy}

✨ Уровень анализа: {astrologer.experience + 1}
"""
        bot.reply_to(message, response)
        astrologer.experience += 1
        
    except Exception as e:
        bot.reply_to(message, f"❌ *Ошибка анализа:* {str(e)}")

print("🔄 ПРОФЕССИОНАЛЬНЫЙ ХОРАРНЫЙ ИМПЕРАТОР запущен...")
print("🌐 HTTP-сервер здоровья работает на порту 5000")

try:
    print("🔗 Подключаемся к Telegram...")
    time.sleep(5)
    bot.remove_webhook()
    print("✅ Webhook удален")
    time.sleep(2)
    print("🔄 Запускаем polling...")
    bot.polling(none_stop=True, timeout=60, long_polling_timeout=60)
except Exception as e:
    print(f"❌ Ошибка подключения: {e}")
    print("🔄 Перезапуск через 15 секунд...")
    time.sleep(15)
